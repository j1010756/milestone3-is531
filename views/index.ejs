<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <title>Paradise Donut Company</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f9f9f9;
        color: #333;
      }

      header {
        background-color: white;
        color: white;
        padding: 20px 0;
        text-align: center;
      }

      header h1 {
        margin: 0;
        font-size: 2.5rem;
        letter-spacing: 1px;
        color: #e67e22;
      }

      .about-us {
        margin: 40px auto;
        width: 80%;
        max-width: 800px;
        text-align: center;
        font-size: 1.1rem;
        line-height: 1.6;
        color: #555;
      }

      h2 {
        text-align: center;
        margin-top: 30px;
        color: #444;
        font-size: 2rem;
        color: #e67e22;
      }

      .donut-list {
        list-style-type: none;
        padding-left: 0;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
      }

      .donut-item {
        background-color: white;
        border: 1px solid #ddd;
        border-radius: 8px;
        width: 250px;
        padding: 20px;
        text-align: center;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        transition: transform 0.2s ease-in-out;
      }

      .donut-item:hover {
        transform: translateY(-5px);
      }

      .donut-image i {
        font-size: 2rem;
        color: #ff6b6b;
        margin-bottom: 10px;
      }

      .donut-name {
        font-weight: bold;
        font-size: 1.2rem;
        margin-bottom: 10px;
        color: #333;
      }

      .donut-description {
        font-size: 0.95rem;
        color: #777;
        margin-bottom: 10px;
      }

      .donut-type {
        font-size: 0.9rem;
        color: #666;
      }

      footer {
        text-align: center;
        padding: 20px;
        background-color: #ff6b6b;
        color: white;
        margin-top: 50px;
      }
    </style>
  </head>
  <body>
    <!--Header-->
    <header>
      <h1>Paradise Donut Company</h1>
    </header>

    <!--About Us Section-->
    <div class="about-us">
      Welcome to the Paradise Donut Company, where we bake fresh, delicious
      donuts every day! From classic flavors to unique specialty items, we offer
      a wide variety of donuts that cater to every sweet tooth. Our donuts are
      crafted with love, using the finest ingredients to ensure every bite is
      heavenly. Whether you're looking for a morning treat or a sweet snack, we
      have the perfect donut waiting for you!
    </div>

    <center><h3>Our Donut List</h3></center>
    <!--Donut List Section-->
    <ul class="donut-list">
      <% function capitalizeFirstLetter(string) { return
      string.charAt(0).toUpperCase() + string.slice(1).toLowerCase(); } %> <%
      for (let i = 0; i < aDonutList.length; i++) { %>
      <li class="donut-item">
        <div class="donut-image">
          <i class="fas fa-circle"></i>
        </div>
        <div class="donut-name">
          <%= capitalizeFirstLetter(aDonutList[i].name) %>
        </div>
        <div class="donut-description">
          Calories: <%= aDonutList[i].description %>
        </div>
        <div class="donut-type">
          Type: <%= capitalizeFirstLetter(aDonutList[i].type) %>
        </div>
      </li>
      <% } %>
    </ul>

    <!--Footer-->
    <footer>&copy; 2024 Paradise Donut Company. All rights reserved.</footer>
  </body>
</html>
